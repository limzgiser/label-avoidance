const DATA_COUNT = 22

const LABEL_OFFSET_Y = 3        // 标注距离标注线的偏移量

const THINKNESS = 23            // 标注线的厚度

const MOVE_X_TIMES = 3          // X 方向避让检测次数 正逆向分别 MOVE_X_TIMES 次

export {
    DATA_COUNT,
    LABEL_OFFSET_Y,
    THINKNESS,
    MOVE_X_TIMES
}